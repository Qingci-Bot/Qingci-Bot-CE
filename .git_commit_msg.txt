feat: 会话阶梯（多轮交互）— Dispatcher 续接调度

- Dispatcher 新增会话阶梯调度：维护挂起中的 _PendingStep（会话键 ->
  matcher + 跨轮 Session 实例），消息事件先查续接，命中则跳过
  rule/permission 直接执行 handler；handler 调用 pause() 挂起等下一条
  同会话消息、finish() 结束阶梯、reject() 拒绝继续等；阶梯默认 300s
  超时自动失效；handler 正常返回则阶梯自然结束
- Session 跨轮复用同一实例（保留 handler 挂载的自定义状态），发送通道
  经 _rebind_send 重绑到当前消息上下文
- PluginManager 卸载/禁用插件时调用 dispatcher.clear_steps_for 清理其
  名下挂起阶梯；bot/plugin/session.py 薄转发 SDK Session API
- TestBot 补充 _send_reply（阶梯文本走主动发送通道），与真实 Bot 对齐
- 插件模板新增 /wizard 会话阶梯示例
- 测试 test_session_steps.py 7 用例：pause 续接 / finish 结束 / reject
  拒绝 / 会话隔离 / 超时失效 / finish 清理 / 卸载清理