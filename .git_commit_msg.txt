style: Web UI 视觉一致性大修（消除内联样式乱象）

- 卡片间距统一：全局 .page-body > .card + .card 邻接规则，删除
  全部 16 处内联 margin-top: 22px（含 grid 后卡片间距）
- toast 单一系统：useToast 改模块级单例（App.vue 统一渲染顶部
  悬浮通知），删除 6 处页面内 toast 渲染与 5 份重复过渡动画；
  页内常驻提示改 .status-bar 类
- 控件全局化：.switch/.slider 收敛到 main.css（删 Settings/
  PluginManager scoped 重复），GroupConfig 原生 checkbox 统一
  为 switch；全局新增 .btn-accent/.btn-warning/.text-muted/
  .text-secondary，修复 PluginManager 悬空 .btn-accent 与危险红
  .btn-warning 语义（禁用=黄、更新=primary）
- 清理旧主题残留：--primary-color/--text-color 加全局别名，
  MessageLog/ChatConfig 焦点色归位 --blue，#6f8ffc 清零
- 布局统一：grid-4 断点统一（1100px→2列/640px→1列）；Dashboard
  卡中卡消除、统计字号统一、启停按钮统一 btn-sm；/setup 首次
  向导全屏化；App.vue 内联样式类化；tab 视觉统一；日志容器
  横向滚动修复
- 构建通过，后端 253 tests 无回归