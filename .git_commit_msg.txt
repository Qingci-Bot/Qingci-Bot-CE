feat: WebUI 平台状态展示（多平台适配器状态可视化）

- 后端：bot.get_status() 新增 platforms 数组（name/display_name/
  connected/last_heartbeat/self_id），未启动 fallback 返回空列表
- WebUI：store 新增 platforms 状态（fetchStatus 拉取）；侧边栏状态
  badge 下方新增平台状态列表——状态点（绿/黄/灰）+ 展示名 +
  在线/离线文本，tooltip 显示 self_id 与心跳相对时间（刚刚/x分钟前）
- TestBot.get_status 与真实 Bot 对齐（供测试断言）；FakeConnection
  继承 PlatformAdapter 补全 name/display_name/is_connected/
  last_heartbeat 契约属性
- 测试：test_platforms.py 增 test_get_status_platforms（多平台状态
  内容断言）；test_api.py 状态端点平台字段结构断言；全套 253 passed