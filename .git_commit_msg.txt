feat: 插件市场体验打磨（icon/homepage/依赖/标签/卸载/索引时间）

- 索引条目扩展：icon（emoji 卡片图标）/ homepage（主页链接）/
  requirements（依赖展示）/ tags（标签筛选），缺省字段回退空值
- 新增 GET /api/plugins/market/info：市场名称/插件数/索引更新时间；
  MarketClient 记录墙钟 fetched_wall（修复原 monotonic 时间被误当
  epoch 的问题），磁盘 meta 回退
- WebUI 市场 Tab 增强：
  - 市场名 + 索引更新时间展示
  - 标签筛选栏（全部/各标签）
  - 卡片图标、依赖标签、主页链接（新窗口）
  - 加载失败显示重试按钮
  - 已安装插件新增「卸载」入口（复用 DELETE /api/plugin/{name}）
- 官方市场索引 hello 条目补充 icon/homepage/requirements/标签并推送
- 测试 test_market.py 增至 10 用例（新字段解析/缺省回退/market_info）；
  全套 239 passed